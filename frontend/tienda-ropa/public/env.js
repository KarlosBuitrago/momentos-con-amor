// Variables de entorno para el frontend
// Este archivo se genera dinámicamente en tiempo de build o se puede configurar manualmente
(function(window) {
  window.env = window.env || {};

  // API URL - se puede sobrescribir con variable de entorno
  window.env.API_URL = window.env.API_URL || 'https://momentos-con-amor.onrender.com/api';
})(this);
